@if(User.Identity.IsAuthenticated)
{
    <form method="post" class="form-inline" asp-page="/logout">
        Bem vindo @User.Identity.Name
        <button type="submit" class="ml-2 btn btn-link">Sair</button>
    </form>
}
else
{
    <a class="btn btn-link" asp-page="/login"></a>
}